<h1><slot /></h1>

<style lang="scss">
  @import "../styles/_variables.scss";

  @keyframes glow {
    from {
      text-shadow: 0px 0px 2px var(--primary), 0px 0px 5px var(--secondary);
    }
    to {
      text-shadow: 0px 0px 5px var(--primary), 0px 0px 20px var(--secondary);
    }
  }

  @keyframes glow-mobile {
    from {
      text-shadow: 0px 0px 0px var(--primary), 0px 0px 5px var(--secondary);
    }
    to {
      text-shadow: 0px 0px 1px var(--primary), 0px 0px 20px var(--secondary);
    }
  }
  h1 {
    color: var(--white);
    font-size: 5rem;
    animation-name: glow;
    animation-duration: 3s;
    animation-iteration-count: infinite;
    animation-direction: alternate;
    font-size: 5rem;
    margin: 0;

    @media screen and (max-width: $mobile-breakpoint) {
      font-size: 5rem;
      animation-name: glow-mobile;
    }

    @container hello (max-width: 700px) {
      font-size: 3rem;
    }
  }
</style>
