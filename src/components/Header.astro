<header>
  <button id="contact" aria-label="Open contact form" tabindex="0"
    >Contact me</button
  >
</header>

<style lang="scss">
  header {
    display: inline-flex;
    justify-content: flex-end;
    grid-row: 1;
    grid-column: 1 / span 2;
    background-color: var(--primary);
    border-bottom: 1px solid var(--secondary);

    button {
      border: none;
      padding: 0 3rem;
      text-transform: uppercase;
      font-weight: bold;
      background: var(--secondary);
      color: var(--primary);
      cursor: pointer;
    }
  }
</style>
